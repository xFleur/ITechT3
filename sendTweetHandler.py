# TODO handle the sending of tweets. This is also the place where we
#   would catch errors and handle them when something goes wrong.
def sendTweet(tweetString, name):
    print("@"+ name + " " + tweetString)